use vitcampus;
create table person(
id int not null,
firstname varchar(256) not null,
lastname varchar(50) not null,
age int );
desc person;
ALTER TABLE person MODIFY lastname VARCHAR(50);
alter table person add constraint unique(id);
alter table person drop constraint age;
ALTER TABLE Person
ADD UNIQUE (AGE);

ALTER TABLE Person
DROP CONSTRAINT Age;
desc person;
CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int,
    CHECK (Age>=18)
);
ALTER TABLE Persons	ADD CHECK (Age>=18);
CREATE TABLE Persons1 (
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int,
    city varchar(10) default 'coimbatore'
);
CREATE TABLE Persons2 (
    ID int primary key,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int,
    city varchar(10) default 'coimbatore'
);
alter table Persons2 drop primary key;
alter table Persons2 add primary key(id);
create table category(
cid int primary key,
cname varchar(100) not null
);
desc products;

insert into products values (102,"furniture",19930);
select * from products;
ALTER TABLE Persons
ADD PRIMARY KEY (ID);
ALTER TABLE Persons
DROP PRIMARY KEY;
drop table products;
create table category1(
cid int primary key,
cname varchar(100) not null
);
insert into category1 values (101,'electronics'),(102,'Home App');
select * from category1;
drop table products;
CREATE TABLE products1 (
    pid int primary key,
    pname varchar(100) NOT NULL,
    cid int, 
    FOREIGN KEY (cid) REFERENCES category(cid)
);
desc products1;
delete from category where cid=101;
insert into products1 values (101,'Power Bank Samsung',101);
insert into products1 values (102,'cupboard',102);
insert into products1 values (101,'Power Bank ',101);
select * from products1;
select * from category;
insert into category values(101,'Electronics');